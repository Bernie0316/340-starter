<%# Assignment w03%>
<% if (title) { %>  <!-- uses a EJS code block to contain a JavaScript if structure to determine if a "title" variable exists. -->
<!-- Display the vehicle make and model in the title element and in the main content heading. -->
<h1><%= title %></h1> <!-- if the variable exists, it is displayed within an <h1> element. -->
<% } else {     
  res.redirect('/') // redirects the response object to the default index route, to deliver the home view.
} %>

<%# messages() %>
<%- itemDetail || "Try" %>